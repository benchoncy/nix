---
description: Build a consensus plan through iterative challenge rounds
agent: plan
---
Load and use the `consensus-plan` skill.

If the user supplied extra input, incorporate it:
$ARGUMENTS

Use current conversation context as the source material and execute the skill's full challenge loop with the `consensus-planning` agent until convergence criteria are met.

Return the final result in the skill's output format.
